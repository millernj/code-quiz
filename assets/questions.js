let questions = [
  {
    question: '<b>Finish this statement:</b> In javascript, the <code>==</code> operator differs from the <code>=</code> operator in that  #blank.',
    options: [
      'the <code>==</code> operator compares values while <code>=</code> assigns them',
      'the <code>==</code> operator assigns values while <code>=</code> compares them',
      'the <code>==</code> operator assigns values globally while <code>=</code> assigns them locally',
      'the <code>==</code> operator assigns values locally while <code>=</code> assigns them globally',
    ],
    answer: 'the <code>==</code> operator compares values while <code>=</code> assigns them'
  },
  {
    question: 'Which of these statements initailize an empty array?',
    options: [
      '<code>var array = Array();</code>',
      '<code>var array = [];</code>',
      '<code>var array = \'\'.split(\'\');</code>',
      'All of the above'
    ],
    answer: 'All of the above'
  }
]